'use strict';

// Открытие-закрытие диалога
window.initializePins.mapHandlerClick();
window.initializePins.mapHandlerKeydown();
window.initializePins.closeClick();
window.initializePins.closeKeydown();

// Валидация формы

window.validation();

// Зависимость полей

window.useFields();
